==================
Django Relative SoftDeletion
==================

Django Relative SoftDeletion is a Django app to restrict permanent deleting of data from database, instead it will add an additional column to mark if any data is deleted or not.